const SkillsTitle = ({ title }) => {
  return (
    <div className="skills__container3">
      <div className="skills__container4">
        <h2 className="skills__title">{title}</h2>
      </div>
    </div>
  );
};

export default SkillsTitle;
